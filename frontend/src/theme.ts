import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#1E8E3E' // Google Classroom yeşili
        },
        secondary: {
            main: '#4285F4' // Mavi
        },
        background: {
            default: '#ffffff'
        }
    }
});

export default theme;
